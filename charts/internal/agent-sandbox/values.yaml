# Default values for agent-sandbox controller
images:
  agent-sandbox: image-repository:image-tag

controller:
  pullPolicy: IfNotPresent
  replicas: 1

service:
  ports:
    metrics:
      port: 8080
      targetPort: 8080
    webhookServer:
      port: 9443
      targetPort: 9443
